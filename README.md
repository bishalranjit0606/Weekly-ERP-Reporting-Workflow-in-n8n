# Weekly-ERP-Reporting-Workflow-in-n8n
Multi-path n8n workflow that handles weekly ERP data extraction, applies conditional logic, and automatically routes the results to two different destinations: a detailed tracking system (Airtable) and a high-level performance report (Discord).
